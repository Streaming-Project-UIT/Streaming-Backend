---
apiVersion: v1
kind: Service
metadata:
  name: springapp
spec:
  type: LoadBalancer
  selector:
    app: springapp
  ports:
  - port: 80
    targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: springapp
  labels:
    app: springapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: gke_springapp
  template:
    metadata:
      labels:
        app: gke_springapp
    spec:
      containers:
      - name: springapp
        image: asia-east1-docker.pkg.dev/$GOOGLE_PROJECT/springapp/gke_springapp:1.0